import { css } from "@emotion/react";
import { theme } from "@/styles/theme";
import { mq } from "@/styles/mq";

export const technicalBoxCss = (color) =>
  css({
    width: "100%",
    height: "100%",
  });
